name: Verify AV Passport

on:
  pull_request:
    branches: [main]
    paths:
      - "index.html"
      - ".github/workflows/verify-av-passport.yml"
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Verify candidate custom domain
        shell: bash
        run: |
          set -euo pipefail
          URL="https://pasaporte.alfombraverde.com.ar"
          BODY="$(curl -fsSL --max-time 20 "$URL")"
          printf '%s' "$BODY" | grep -q "AV DOMAIN TEST"
          echo "VERIFIED $URL"
